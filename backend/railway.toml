[build]
builder = "nixpacks"

[deploy]
startCommand = "echo $FIREBASE_CREDENTIALS_JSON > /app/firebase-credentials.json && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
